# Branched extraction package.
# Entry points:
#   extraction.project_runner.run_project()   — full project run
#   extraction.suggest.suggest_branches()     — auto-suggest from document sample
#   extraction.branch_config.*               — data models
